<!--
CO_OP_TRANSLATOR_METADATA:
{
  "original_hash": "66aa3b998a0c5c69721c661bdaaf6830",
  "translation_date": "2025-09-22T12:26:21+00:00",
  "source_file": "Module08/04.CuttingEdgeModels.md",
  "language_code": "ja"
}
-->
# セッション4: 最新モデル – LLMs、SLMs、オンデバイス推論

## 概要

LLMsとSLMsを比較し、ローカル推論とクラウド推論のトレードオフを評価します。また、PhiとONNX Runtimeを使用してEdgeAIシナリオを実現するデモを実装します。さらに、Chainlit RAG、WebGPU推論オプション、Open WebUI統合についても紹介します。

参考資料:
- Foundry Local ドキュメント: https://learn.microsoft.com/en-us/azure/ai-foundry/foundry-local/
- ONNX Runtime GenAI: https://onnxruntime.ai/
- Open WebUIの使い方 (Open WebUIを使用したチャットアプリ): https://learn.microsoft.com/en-us/azure/ai-foundry/foundry-local/how-to/how-to-chat-application-with-open-web-ui

## 学習目標
- LLMとSLMのコスト、レイテンシー、精度に関するトレードオフを理解する
- 特定のビジネスニーズに応じてローカル推論とクラウド推論を選択する
- Chainlitを使用して小規模なRAGデモを実装する
- ブラウザ側での加速のためにWebGPUを探る
- Open WebUIをFoundry Localに接続する

## パート1: LLM vs SLM – 意思決定マトリックス

考慮事項:
- レイテンシー: SLMはオンデバイスでサブセカンドの応答を提供することが多い
- コスト: ローカル推論はクラウドコストを削減する
- プライバシー: 機密データがオンデバイスに留まる
- 機能: LLMは複雑なタスクでSLMを上回る可能性がある
- 信頼性: ハイブリッド戦略はダウンタイムリスクを軽減する

## パート2: ローカル vs クラウド – ハイブリッドパターン

- ローカル優先でクラウドをフォールバックとして使用（大規模/複雑なプロンプト向け）
- クラウド優先でローカルを使用（プライバシー重視やオフラインシナリオ向け）
- タスクタイプによるルーティング（コード生成はDeepSeekへ、一般的なチャットはPhi/Qwenへ）

## パート3: Chainlitを使用したRAGチャットアプリ（最小構成）

依存関係をインストール:
```powershell
python -m venv .venv
.\.venv\Scripts\activate
pip install chainlit requests
```

`app.py`:
```python
import chainlit as cl
import requests

BASE_URL = "http://localhost:8000"
MODEL = "phi-4-mini"

@cl.on_chat_start
async def start():
    await cl.Message(content="Welcome to Local RAG Chat!").send()

@cl.on_message
async def main(message: cl.Message):
    # Basic passthrough to Foundry Local (extend with retrieval)
    payload = {
        "model": MODEL,
        "messages": [{"role": "user", "content": message.content}],
        "max_tokens": 300
    }
    r = requests.post(f"{BASE_URL}/v1/chat/completions", json=payload, timeout=60)
    r.raise_for_status()
    content = r.json()["choices"][0]["message"]["content"]
    await cl.Message(content=content).send()
```

実行:
```powershell
# Ensure a model is running
foundry model run phi-4-mini

# Launch Chainlit
chainlit run app.py -w
```

拡張: シンプルなリトリーバー（ローカルファイル）を追加し、取得したコンテキストをユーザープロンプトに前置します。

## パート4: WebGPU推論（概要）

WebGPUを使用してブラウザ内で小規模モデルを直接実行します。これはプライバシー重視のデモやゼロインストール体験に最適です。以下はONNX Runtime WebとWebGPU実行プロバイダーを使用した最小構成のステップバイステップ例です。

1) WebGPUのサポートを確認
- Chromiumブラウザ: chrome://gpu → “WebGPU”が有効であることを確認
- プログラムによる確認（コード内でも確認します）: `if (!('gpu' in navigator)) { /* no WebGPU */ }`

2) 最小プロジェクトを作成
フォルダを作成し、2つのファイルを作成: `index.html` と `main.js`

`index.html`
```html
<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>WebGPU + ONNX Runtime Web (Demo)</title>
    <!-- Load ONNX Runtime Web (WebGPU build) from CDN -->
    <script src="https://cdn.jsdelivr.net/npm/onnxruntime-web/dist/ort.webgpu.min.js"></script>
    <style>
      body { font-family: system-ui, sans-serif; margin: 2rem; }
      pre { background: #f5f5f5; padding: 1rem; overflow:auto; }
    </style>
  </head>
  <body>
    <h1>WebGPU + ONNX Runtime Web</h1>
    <p id="status">Initializing…</p>
    <pre id="output"></pre>
    <script type="module" src="./main.js"></script>
  </body>
</html>
```

`main.js`
```javascript
const statusEl = document.getElementById('status');
const outputEl = document.getElementById('output');

function log(msg) {
  outputEl.textContent += `${msg}\n`;
}

(async () => {
  try {
    if (!('gpu' in navigator)) {
      statusEl.textContent = 'WebGPU not available in this browser.';
      return;
    }
    statusEl.textContent = 'WebGPU detected. Loading model…';

    // Use a tiny identity model (or any small ONNX) hosted remotely.
    // For demo, we synthesize a trivial input and expect the same output shape.
    // Replace with your own model URL if desired.
    const modelUrl = 'https://huggingface.co/onnx/models/resolve/main/vision/classification/mnist-12/mnist-12.onnx?download=true';

    // Create a WebGPU session
    const session = await ort.InferenceSession.create(modelUrl, {
      executionProviders: ['webgpu']
    });
    log('Session created with WebGPU.');

    // Create a dummy input of the correct shape for MNIST (1x1x28x28)
    const inputData = new Float32Array(1 * 1 * 28 * 28);
    // Minimal non-zero to avoid NaNs in some models
    inputData[0] = 1.0;
    const input = new ort.Tensor('float32', inputData, [1, 1, 28, 28]);

    // The MNIST model input name is typically 'Input3' or similar; we can inspect session.inputNames
    const feeds = {};
    const inputName = session.inputNames[0];
    feeds[inputName] = input;

    const results = await session.run(feeds);
    const outputName = session.outputNames[0];
    const output = results[outputName]; // Float32Array logits

    statusEl.textContent = 'Inference complete.';
    log(`Input name: ${inputName}`);
    log(`Output name: ${outputName}`);
    log(`Output length: ${output.data.length}`);

    // Argmax to get predicted digit
    let bestIdx = 0; let bestVal = -Infinity;
    for (let i = 0; i < output.data.length; i++) {
      if (output.data[i] > bestVal) { bestVal = output.data[i]; bestIdx = i; }
    }
    log(`Predicted class (argmax): ${bestIdx}`);
  } catch (e) {
    statusEl.textContent = 'Error during inference.';
    log(e?.stack || String(e));
  }
})();
```

3) ローカルで提供（Windows cmd.exe）
ブラウザがモデルを取得できるように、シンプルな静的サーバーを使用します。

```cmd
REM In cmd.exe
mkdir webgpu-demo
cd webgpu-demo
REM Save index.html and main.js here

REM Option A: Python (if installed)
py -m http.server 5173

REM Option B: Node serve (requires Node.js)
REM npx serve -p 5173 .
```

ブラウザで http://localhost:5173 を開きます。初期化ログ、WebGPUによるセッション作成、argmax予測が表示されるはずです。

4) トラブルシューティング
- WebGPUが利用できない場合: Chrome/Edgeを更新し、GPUドライバーが最新であることを確認し、chrome://flagsで“Enable WebGPU”を確認してください。
- CORSやfetchエラーが発生する場合: ファイルをhttp://（file://ではなく）で提供し、モデルURLがクロスオリジンリクエストを許可していることを確認してください。
- CPUへのフォールバック: `executionProviders: ['wasm']` に変更して基本動作を確認します。

5) 次のステップ
- ドメイン固有のONNXモデル（例: 画像分類や小型テキストモデル）を入れ替える
- 実際の入力に対する前処理/後処理ロジックを追加する
- 大型モデルや本番環境のレイテンシーには、Foundry LocalやONNX Runtime Serverを推奨

## パート5: Open WebUI + Foundry Local（ステップバイステップ）

Open WebUIをFoundry LocalのOpenAI互換エンドポイントに接続し、ローカルチャットUIを構築します。

1) 必要条件
- Foundry Localがインストールされ、動作していること（`foundry --version`）
- ローカルで実行可能なモデルが1つあること（例: `phi-4-mini`）
- Docker Desktopがインストールされていること（Open WebUIに推奨）

2) Foundry Localでモデルを起動
```powershell
foundry model run phi-4-mini
```
これにより、OpenAI互換APIが`http://localhost:8000`で公開されます。

3) Open WebUIを起動（Docker）
```powershell
# Pull and run Open WebUI
docker pull ghcr.io/open-webui/open-webui:main

docker run -d --name open-webui -p 3000:8080 ^
  -e OPENAI_API_BASE_URL=http://host.docker.internal:8000/v1 ^
  -e OPENAI_API_KEY=local-key ^
  ghcr.io/open-webui/open-webui:main
```
注意:
- Windowsでは、`host.docker.internal`を使用してコンテナがホストの`localhost`にアクセスできます。
- `OPENAI_API_BASE_URL`をFoundry Localのエンドポイントに設定し、ダミーの`OPENAI_API_KEY`を使用します。

4) Open WebUI UIから設定（代替方法）
- http://localhost:3000 にアクセス
- 初期セットアップを完了（管理者ユーザー）
- 設定 → モデル/プロバイダーに移動
- Base URLを設定: `http://host.docker.internal:8000/v1`
- API Keyを設定: `local-key`（プレースホルダー）
- 保存

5) テストプロンプトを実行
- Open WebUIチャットでモデル名`phi-4-mini`を選択または入力
- プロンプト: “オンデバイスAI推論の利点を5つ挙げてください。”
- ローカルモデルからストリームされた応答が表示されるはずです

6) トラブルシューティング
```powershell
# Verify Foundry Local is serving and responding
curl http://localhost:8000/v1/models

# Check Docker logs for Open WebUI
docker logs -f open-webui

# If you get ECONNREFUSED, ensure the model is running and port 8000 is open
# If the model name doesn't appear, try typing it manually in Open WebUI
```

7) Open WebUIデータを永続化（オプション）
```powershell
# Stop and remove the container
docker stop open-webui
docker rm open-webui

# Re-run with a volume to persist data
mkdir %USERPROFILE%\openwebui-data

docker run -d --name open-webui -p 3000:8080 ^
  -v %USERPROFILE%\openwebui-data:/app/backend/data ^
  -e OPENAI_API_BASE_URL=http://host.docker.internal:8000/v1 ^
  -e OPENAI_API_KEY=local-key ^
  ghcr.io/open-webui/open-webui:main
```


## 実践チェックリスト
- [ ] SLMとLLMの応答/レイテンシーをローカルで比較
- [ ] Chainlitデモを少なくとも2つのモデルで実行
- [ ] Open WebUIをローカルエンドポイントに接続し、テストを実行

## 次のステップ
- セッション5でのエージェントワークフローに備える
- ハイブリッドローカル/クラウドがROIを向上させるシナリオを特定する

---

